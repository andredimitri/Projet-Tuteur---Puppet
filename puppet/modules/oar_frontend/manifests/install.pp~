class oar_server::install {
package { "oar-common:
ensure => installed,
}
package { "oar-keyring":
ensure => installed,
}
package { "oar-admin:
ensure => installed,
}
package { "oar-server":
ensure => installed,
}
}
