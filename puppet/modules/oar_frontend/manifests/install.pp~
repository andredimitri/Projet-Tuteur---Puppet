class oar_frontend::install {
package { "oar-common:
ensure => installed,
}
package { "oar-keyring":
ensure => installed,
}
package { "oar-doc:
ensure => installed,
}
package { "oar-user":
ensure => installed,
}
package { "oar-node":
ensure => installed,
}
package { "taktuk":
ensure => installed,
}
}
